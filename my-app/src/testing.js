import React from 'react'

function testing() {
 return (
  <div>
   
  </div>
 )
}

export default testing

// với các function dùng để export thì ta không còn uppercase letter cho chữ cái đầu. Việc đó chỉ dùng cho các hàm trực tiếp dùng không cần export 

